{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the local management webview; no shell, filesystem, HTTP or dialog plugin access.",
  "windows": ["main"],
  "permissions": []
}
